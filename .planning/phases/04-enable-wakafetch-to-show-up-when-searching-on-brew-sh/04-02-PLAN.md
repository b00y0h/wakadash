---
phase: 04-repository-setup
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - ../wakadash/.goreleaser.yaml
  - ../wakadash/.github/workflows/release.yml
  - ../wakadash/.github/workflows/build.yml
autonomous: false

must_haves:
  truths:
    - "Creating a version tag triggers GitHub Actions release workflow"
    - "Release artifacts include darwin/linux x amd64/arm64 binaries"
    - "Release includes SHA256 checksums file"
    - "Binaries report correct version when run with --version"
  artifacts:
    - path: "../wakadash/.goreleaser.yaml"
      provides: "GoReleaser build configuration"
      contains: "version: 2"
    - path: "../wakadash/.github/workflows/release.yml"
      provides: "Tag-triggered release workflow"
      contains: "push:"
    - path: "../wakadash/.github/workflows/build.yml"
      provides: "CI build verification"
      contains: "go build"
  key_links:
    - from: "../wakadash/.github/workflows/release.yml"
      to: "../wakadash/.goreleaser.yaml"
      via: "goreleaser-action reads config"
      pattern: "goreleaser-action"
    - from: "../wakadash/.goreleaser.yaml"
      to: "../wakadash/cmd/wakadash/main.go"
      via: "builds.main path"
      pattern: "main: ./cmd/wakadash"
---

<objective>
Configure GoReleaser and GitHub Actions for tag-triggered multi-platform releases.

Purpose: Enable automated release workflow where pushing a version tag produces platform binaries and checksums.
Output: .goreleaser.yaml config, release workflow, and verified release artifacts.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enable-wakafetch-to-show-up-when-searching-on-brew-sh/04-CONTEXT.md
@.planning/phases/04-enable-wakafetch-to-show-up-when-searching-on-brew-sh/04-RESEARCH.md
@.planning/phases/04-enable-wakafetch-to-show-up-when-searching-on-brew-sh/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GoReleaser configuration</name>
  <files>
    ../wakadash/.goreleaser.yaml
  </files>
  <action>
Create .goreleaser.yaml with multi-platform build configuration:

```yaml
# .goreleaser.yaml
version: 2

builds:
  - id: wakadash
    main: ./cmd/wakadash
    binary: wakadash
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.CommitDate}}

archives:
  - id: wakadash
    formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE
      - CHANGELOG.md

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_checksums.txt"
  algorithm: sha256

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'
```

Key configuration notes (per research pitfalls):
- `version: 2` required for GoReleaser v2
- `formats: [tar.gz]` (array, NOT singular `format:`)
- `main: ./cmd/wakadash` matches project structure
- ldflags target `main.version` (NOT full module path)
- NO `brews:` section (Homebrew tap is Phase 7)
- CGO_ENABLED=0 for fully static binaries

Validate with: `goreleaser check`
  </action>
  <verify>
    - `cat ../wakadash/.goreleaser.yaml | grep "version: 2"` confirms v2 format
    - `cat ../wakadash/.goreleaser.yaml | grep "formats:"` confirms array syntax
    - `cd ../wakadash && goreleaser check` reports valid config
  </verify>
  <done>
    GoReleaser config validates successfully, targets 4 platform combinations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions workflows</name>
  <files>
    ../wakadash/.github/workflows/release.yml
    ../wakadash/.github/workflows/build.yml
  </files>
  <action>
Create two GitHub Actions workflows:

**release.yml** (tag-triggered releases):
```yaml
name: release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

Key notes (per research pitfalls):
- `fetch-depth: 0` for full history (changelog generation)
- `go-version: stable` gets current stable Go
- GITHUB_TOKEN is sufficient (no HOMEBREW_TAP_TOKEN needed in Phase 4)

**build.yml** (PR/push CI):
```yaml
name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Build
        run: go build -v ./...

      - name: Vet
        run: go vet ./...
```

This provides basic CI verification on every push/PR.
  </action>
  <verify>
    - `ls ../wakadash/.github/workflows/` shows release.yml and build.yml
    - `grep "fetch-depth: 0" ../wakadash/.github/workflows/release.yml` confirms full clone
    - `grep "goreleaser-action@v6" ../wakadash/.github/workflows/release.yml` confirms action version
  </verify>
  <done>
    Both workflows created with correct configuration for tag-triggered releases and PR CI.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test GoReleaser locally and commit all changes</name>
  <files>
    ../wakadash/.goreleaser.yaml
    ../wakadash/.github/workflows/release.yml
    ../wakadash/.github/workflows/build.yml
  </files>
  <action>
Test GoReleaser configuration locally and commit:

1. Run local snapshot build:
   ```bash
   cd ../wakadash
   goreleaser release --snapshot --clean
   ```

2. Verify version injection in built binary:
   ```bash
   ./dist/wakadash_linux_amd64_v1/wakadash --version
   # Should show version with snapshot info
   ```

3. Verify all 4 platform artifacts exist:
   ```bash
   ls dist/*.tar.gz
   # Should show darwin_amd64, darwin_arm64, linux_amd64, linux_arm64
   ```

4. Verify checksums file:
   ```bash
   cat dist/*_checksums.txt
   ```

5. Commit all release automation files:
   ```bash
   git add .goreleaser.yaml .github/
   git commit -m "feat(release): add GoReleaser and GitHub Actions workflows

   - Configure GoReleaser v2 for darwin/linux x amd64/arm64
   - Add release workflow triggered on version tags
   - Add build workflow for PR/push CI verification
   - Includes SHA256 checksums for all artifacts"
   git push origin main
   ```

6. Clean up dist/ (should be gitignored):
   ```bash
   rm -rf dist/
   ```
  </action>
  <verify>
    - Local `goreleaser release --snapshot --clean` succeeds
    - `ls dist/*.tar.gz | wc -l` shows 4 archives (before cleanup)
    - `git log -1 --oneline` shows the release automation commit
    - `git status` shows clean working tree
  </verify>
  <done>
    GoReleaser builds all 4 platforms locally, all release automation committed and pushed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify release workflow with v0.1.0 tag</name>
  <action>
    Push v0.1.0 tag to trigger release workflow and verify artifacts.
  </action>
  <verify>
    1. Push a test tag to trigger the release workflow:
       ```bash
       cd ../wakadash
       git tag -a v0.1.0 -m "Initial release - Phase 4 complete"
       git push origin v0.1.0
       ```

    2. Watch the GitHub Actions workflow:
       - Visit https://github.com/b00y0h/wakadash/actions
       - Wait for "release" workflow to complete (typically 2-3 minutes)

    3. Verify release artifacts:
       - Visit https://github.com/b00y0h/wakadash/releases
       - Confirm v0.1.0 release exists with:
         - wakadash_0.1.0_darwin_amd64.tar.gz
         - wakadash_0.1.0_darwin_arm64.tar.gz
         - wakadash_0.1.0_linux_amd64.tar.gz
         - wakadash_0.1.0_linux_arm64.tar.gz
         - wakadash_0.1.0_checksums.txt

    4. (Optional) Download and verify a binary:
       ```bash
       # On macOS arm64:
       curl -LO https://github.com/b00y0h/wakadash/releases/download/v0.1.0/wakadash_0.1.0_darwin_arm64.tar.gz
       tar xzf wakadash_0.1.0_darwin_arm64.tar.gz
       ./wakadash --version
       # Should show: wakadash 0.1.0
       ```
  </verify>
  <done>
    User confirms release workflow completed, all 4 platform binaries + checksums.txt present in release.
  </done>
</task>

</tasks>

<verification>
- `goreleaser check` passes
- `goreleaser release --snapshot --clean` produces 4 platform archives
- GitHub Actions release workflow exists at .github/workflows/release.yml
- Pushing a version tag creates a GitHub release with all artifacts
</verification>

<success_criteria>
1. .goreleaser.yaml validates with `goreleaser check`
2. Local snapshot build produces darwin/linux x amd64/arm64 archives
3. Pushing v0.1.0 tag triggers release workflow
4. GitHub release contains 4 .tar.gz archives + checksums.txt
5. Downloaded binary reports correct version with --version
</success_criteria>

<output>
After completion, create `.planning/phases/04-enable-wakafetch-to-show-up-when-searching-on-brew-sh/04-02-SUMMARY.md`
</output>
