---
phase: 04-repository-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../wakadash/go.mod
  - ../wakadash/go.sum
  - ../wakadash/.gitignore
  - ../wakadash/LICENSE
  - ../wakadash/README.md
  - ../wakadash/CONTRIBUTING.md
  - ../wakadash/CHANGELOG.md
  - ../wakadash/cmd/wakadash/main.go
  - ../wakadash/internal/types/types.go
  - ../wakadash/internal/config/config.go
  - ../wakadash/internal/api/client.go
autonomous: false

must_haves:
  truths:
    - "User can clone b00y0h/wakadash repository"
    - "User can build with 'go build ./cmd/wakadash'"
    - "Running 'wakadash --help' shows usage information"
    - "Running 'wakadash --version' shows version details"
  artifacts:
    - path: "../wakadash/go.mod"
      provides: "Go module definition"
      contains: "module github.com/b00y0h/wakadash"
    - path: "../wakadash/cmd/wakadash/main.go"
      provides: "CLI entry point with --help and --version"
      min_lines: 30
    - path: "../wakadash/internal/api/client.go"
      provides: "WakaTime API client"
      min_lines: 50
    - path: "../wakadash/internal/config/config.go"
      provides: "~/.wakatime.cfg reader"
      min_lines: 40
    - path: "../wakadash/internal/types/types.go"
      provides: "API response types"
      min_lines: 50
    - path: "../wakadash/LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: "../wakadash/README.md"
      provides: "Project documentation with wakafetch attribution"
      contains: "wakafetch"
  key_links:
    - from: "../wakadash/cmd/wakadash/main.go"
      to: "../wakadash/internal/config"
      via: "import for config loading"
      pattern: "github.com/b00y0h/wakadash/internal/config"
    - from: "../wakadash/internal/api/client.go"
      to: "../wakadash/internal/types"
      via: "import for response types"
      pattern: "github.com/b00y0h/wakadash/internal/types"
---

<objective>
Create the b00y0h/wakadash GitHub repository and port wakafetch code via clean rewrite with proper attribution.

Purpose: Establish the standalone wakadash codebase that will become the TUI dashboard in Phase 5.
Output: Public repository with working Go build, CLI stub with --help and --version, ported API/config/types packages.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enable-wakafetch-to-show-up-when-searching-on-brew-sh/04-CONTEXT.md
@.planning/phases/04-enable-wakafetch-to-show-up-when-searching-on-brew-sh/04-RESEARCH.md
@wakafetch/api.go
@wakafetch/config.go
@wakafetch/types/types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub repository and initialize Go module</name>
  <files>
    ../wakadash/go.mod
    ../wakadash/go.sum
    ../wakadash/.gitignore
    ../wakadash/LICENSE
    ../wakadash/README.md
    ../wakadash/CONTRIBUTING.md
    ../wakadash/CHANGELOG.md
  </files>
  <action>
Create the repository and documentation:

1. Create GitHub repo using gh CLI:
   ```bash
   gh repo create b00y0h/wakadash --public \
     --description "A live terminal dashboard for WakaTime coding stats" \
     --disable-wiki --disable-projects
   ```

2. Clone and initialize:
   ```bash
   gh repo clone b00y0h/wakadash ../wakadash
   cd ../wakadash
   go mod init github.com/b00y0h/wakadash
   ```

3. Create .gitignore with:
   - dist/ (GoReleaser output)
   - *.test
   - .DS_Store
   - wakadash (binary)

4. Create LICENSE (MIT with b00y0h as copyright holder, current year)

5. Create README.md with:
   - Title: wakadash
   - Tagline: "Like htop for your coding activity"
   - Description: Live terminal dashboard for WakaTime coding stats
   - Install from source instructions (go install)
   - Usage: wakadash, wakadash --help, wakadash --version
   - Attribution: "Inspired by [wakafetch](https://github.com/sahaj-b/wakafetch) by sahaj-b"
   - License section

6. Create CONTRIBUTING.md with basic contribution guidelines

7. Create empty CHANGELOG.md (will be populated by GoReleaser)

Use `go 1.21` as the minimum Go version in go.mod (per research recommendation - oldest LTS with needed features).
  </action>
  <verify>
    - `ls ../wakadash/` shows go.mod, .gitignore, LICENSE, README.md, CONTRIBUTING.md, CHANGELOG.md
    - `gh repo view b00y0h/wakadash --json name,visibility` shows public repo
    - `cat ../wakadash/go.mod` shows module github.com/b00y0h/wakadash
    - `grep wakafetch ../wakadash/README.md` shows attribution
  </verify>
  <done>
    GitHub repository exists, Go module initialized, all documentation files present with wakafetch attribution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port types, config, and API packages via clean rewrite</name>
  <files>
    ../wakadash/internal/types/types.go
    ../wakadash/internal/config/config.go
    ../wakadash/internal/api/client.go
  </files>
  <action>
Port the three core packages from wakafetch using clean rewrite approach (per user decision - not copy/paste):

**internal/types/types.go:**
- Rewrite types from wakafetch/types/types.go
- Include: StatItem, DayData, SummaryResponse, StatsResponse
- Use github.com/b00y0h/wakadash/internal/types as package path
- Add attribution comment: "// Attribution: Rewritten from github.com/sahaj-b/wakafetch (MIT License)"

**internal/config/config.go:**
- Rewrite config loading from wakafetch/config.go
- Config struct with APIURL and APIKey fields
- Load() function reads ~/.wakatime.cfg
- Include Wakapi URL normalization (https://wakapi.dev/api -> https://wakapi.dev/api/compat/wakatime)
- Use bufio.Scanner + strings.Cut for INI parsing (no external deps)
- Add attribution comment

**internal/api/client.go:**
- Rewrite API client from wakafetch/api.go
- Client struct with APIKey and APIURL fields
- FetchStats(rangeStr string) for /v1/users/current/stats/{range}
- FetchSummary(days int) for /v1/users/current/summaries
- Use generic fetchJSON[T] helper for HTTP calls
- Handle Wakapi URL path prefix in buildURL
- 10 second timeout
- Add attribution comment

Key differences from wakafetch (clean rewrite):
- Use github.com/b00y0h/wakadash module paths throughout
- Use strings.Cut instead of strings.SplitN
- Struct-based Client instead of functions with global state
- Error wrapping with %w for better error chains
  </action>
  <verify>
    - `go build ./internal/...` (from ../wakadash) succeeds
    - `go vet ./internal/...` reports no issues
    - `grep "Attribution:" ../wakadash/internal/*/client.go ../wakadash/internal/*/config.go` shows attribution comments
    - `grep "github.com/b00y0h/wakadash" ../wakadash/internal/api/client.go` shows correct import path
  </verify>
  <done>
    Three internal packages compile, use correct module paths, include attribution comments.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create CLI entry point with --help and --version</name>
  <files>
    ../wakadash/cmd/wakadash/main.go
  </files>
  <action>
Create the CLI entry point as a minimal stub:

**cmd/wakadash/main.go:**
- package main
- Import flag, fmt, os, runtime, and github.com/b00y0h/wakadash/internal/config
- Version injection variables (version, commit, date) with defaults for dev builds
- --version flag: shows "wakadash {version}\n  commit: {commit}\n  built: {date}\n  go: {runtime.Version()}"
- Custom flag.Usage for --help with:
  - "Usage: wakadash [options]"
  - "A live terminal dashboard for WakaTime coding stats."
  - "Options:" followed by flag.PrintDefaults()
- Main logic (per user decision - dashboard launches immediately):
  1. Parse flags
  2. If --version, print version info and exit 0
  3. If --help (handled by flag.Usage automatically)
  4. Otherwise, attempt to load config
  5. If config load fails, print helpful error and exit 1
  6. If config loads, print stub message: "wakadash: dashboard launching... (Phase 5)"

The stub does NOT actually launch a TUI (that's Phase 5). It validates the config exists and exits.
  </action>
  <verify>
    - `go build -o wakadash ./cmd/wakadash` (from ../wakadash) succeeds
    - `./wakadash --help` shows usage with "live terminal dashboard" in output
    - `./wakadash --version` shows "wakadash dev" and go version
    - `./wakadash` attempts config load (may fail if ~/.wakatime.cfg doesn't exist - that's OK)
  </verify>
  <done>
    CLI binary builds, --help shows usage, --version shows version info, running without flags attempts config load.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify repository and CLI functionality</name>
  <action>
    Human verification of the created repository and CLI stub.
  </action>
  <verify>
    1. Visit https://github.com/b00y0h/wakadash - verify repo exists and is public
    2. Check README shows "htop for your coding activity" tagline and wakafetch attribution
    3. Run from the wakadash directory:
       ```bash
       cd ../wakadash
       go build ./cmd/wakadash
       ./wakadash --help
       ./wakadash --version
       ```
    4. Verify --help shows "live terminal dashboard" description
    5. Verify --version shows "wakadash dev" with go version
  </verify>
  <done>
    User confirms repository is public, README has correct content, and CLI stub functions correctly.
  </done>
</task>

</tasks>

<verification>
- GitHub repository b00y0h/wakadash exists and is public
- `go build ./cmd/wakadash` succeeds from the wakadash directory
- `./wakadash --help` shows usage information
- `./wakadash --version` shows version details
- Attribution to wakafetch present in README.md and source code comments
</verification>

<success_criteria>
1. Repository b00y0h/wakadash is cloneable by anyone
2. `go build ./cmd/wakadash` produces a working binary
3. `wakadash --help` displays usage with "live terminal dashboard" description
4. `wakadash --version` displays version, commit, build date, and Go version
5. README.md credits wakafetch as inspiration
</success_criteria>

<output>
After completion, create `.planning/phases/04-enable-wakafetch-to-show-up-when-searching-on-brew-sh/04-01-SUMMARY.md`
</output>
