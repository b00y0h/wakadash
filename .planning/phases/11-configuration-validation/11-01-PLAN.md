---
phase: 11-configuration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wakadash/internal/config/config.go
  - wakadash/cmd/wakadash/main.go
autonomous: true
requirements: [CFG-01]

must_haves:
  truths:
    - "User can add history_repo key to ~/.wakatime.cfg in [wakadash] section"
    - "Dashboard starts successfully when history_repo is not configured"
    - "Dashboard starts successfully when history_repo is configured but invalid"
  artifacts:
    - path: "wakadash/internal/config/config.go"
      provides: "HistoryRepo field and section-aware parsing"
      contains: "HistoryRepo"
    - path: "wakadash/cmd/wakadash/main.go"
      provides: "Graceful handling of missing/invalid history_repo"
      min_lines: 20
  key_links:
    - from: "wakadash/cmd/wakadash/main.go"
      to: "config.Load()"
      via: "Config struct with HistoryRepo field"
      pattern: "cfg\\.HistoryRepo"
---

<objective>
Add `history_repo` configuration support with graceful fallback behavior.

Purpose: Enable users to specify their GitHub archive repository for historical data, while maintaining backwards compatibility with existing configs and ensuring the dashboard never crashes due to missing or invalid configuration.

Output: Extended Config struct with HistoryRepo field, section-aware INI parsing for `[wakadash]` section, and graceful startup behavior.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-configuration-validation/11-CONTEXT.md

# Reference existing config patterns
@wakadash/internal/config/config.go
@wakadash/internal/theme/config.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Config struct and add section-aware parsing</name>
  <files>wakadash/internal/config/config.go</files>
  <action>
Extend the existing Config struct and Load function to support `[wakadash]` section:

1. **Add HistoryRepo field to Config struct:**
   ```go
   type Config struct {
       APIURL      string
       APIKey      string
       HistoryRepo string  // e.g., "b00y0h/wakatime-data"
   }
   ```

2. **Implement section-aware INI parsing:**
   - Track current section as scanning proceeds (default: empty string for root/global)
   - When line matches `[section_name]`, update current section
   - Only parse `history_repo` key when in `[wakadash]` section
   - Continue parsing `api_url` and `api_key` from root/global section

3. **Parse history_repo value:**
   - In `[wakadash]` section, match key `history_repo`
   - Store raw value (e.g., `b00y0h/wakatime-data`) - no URL normalization in this phase
   - HistoryRepo defaults to empty string if not configured

4. **Maintain backward compatibility:**
   - Existing configs without `[wakadash]` section continue to work
   - api_url and api_key validation unchanged (required fields)
   - HistoryRepo is optional - no error if missing

Example config format after this change:
```ini
api_url = https://wakapi.dev/api
api_key = waka_xxxxx

[wakadash]
theme = dracula
history_repo = b00y0h/wakatime-data
```

Note: The existing theme= parsing in internal/theme/config.go uses separate functions. This phase focuses on extending the main config package. Future phase may consolidate.
  </action>
  <verify>
    <automated>cd /workspace/wakadash && CGO_ENABLED=0 go build ./... && go vet ./...</automated>
    <manual>Inspect config.go for HistoryRepo field and section-aware parsing logic</manual>
  </verify>
  <done>
Config struct has HistoryRepo field. Load() parses [wakadash] section and extracts history_repo value. Config without history_repo returns empty string (no error).
  </done>
</task>

<task type="auto">
  <name>Task 2: Ensure graceful startup with missing/invalid history_repo</name>
  <files>wakadash/cmd/wakadash/main.go</files>
  <action>
Verify and ensure the dashboard starts gracefully regardless of history_repo state:

1. **Review existing main.go startup flow:**
   - Config loading already handles missing api_url/api_key with errors
   - Dashboard startup should NOT fail when HistoryRepo is empty
   - Dashboard startup should NOT fail when HistoryRepo has invalid format

2. **No changes needed if graceful already:**
   - Since HistoryRepo is optional (not validated), startup is inherently graceful
   - Empty HistoryRepo simply means no historical data support (Phase 12 concern)
   - Invalid format (e.g., "not-a-valid-repo") will be handled in Phase 12 when fetching

3. **Verify startup behavior:**
   - Build and run dashboard to confirm it starts without history_repo
   - The dashboard should display live stats from API as normal

4. **Document for Phase 12:**
   - The actual validation of history_repo format (owner/repo pattern) will happen when attempting to fetch from GitHub in Phase 12
   - This phase only adds config reading capability

Note: Per CONTEXT.md, validation behavior is Claude's discretion. Since Phase 12 handles actual fetching, we defer validation to that phase for simplicity and better error context.
  </action>
  <verify>
    <automated>cd /workspace/wakadash && CGO_ENABLED=0 go build -o /tmp/wakadash ./cmd/wakadash && /tmp/wakadash --version</automated>
    <manual>Run wakadash without history_repo configured and verify it starts normally showing live stats</manual>
  </verify>
  <done>
Dashboard builds successfully and starts without crashing when history_repo is not configured. Version flag outputs correctly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Auto-create [wakadash] section template on first run</name>
  <files>wakadash/internal/config/config.go</files>
  <action>
Add function to auto-create `[wakadash]` section with commented template:

1. **Add EnsureWakadashSection function:**
   ```go
   // EnsureWakadashSection adds a [wakadash] section to ~/.wakatime.cfg if missing.
   // Called on startup to provide user with documented config options.
   func EnsureWakadashSection() error
   ```

2. **Section detection:**
   - Read config file
   - Check if `[wakadash]` section exists (case-insensitive match)
   - If exists, return nil (no modification)

3. **Append section template if missing:**
   ```ini

   [wakadash]
   # Theme: dracula, nord, gruvbox, monokai, solarized, tokyonight
   # theme = dracula

   # GitHub repo for historical data (format: owner/repo)
   # Set up your archive with wakasync: https://github.com/b00y0h/wakasync
   # history_repo = your-username/wakatime-data
   ```

4. **Call on startup:**
   - In main.go, after successful config load, call `config.EnsureWakadashSection()`
   - Handle error gracefully (log warning, continue execution)
   - This provides discoverability without blocking startup

5. **File handling:**
   - Preserve existing file content
   - Append with leading newline for separation
   - Use 0600 permissions (matching theme/config.go pattern)
  </action>
  <verify>
    <automated>cd /workspace/wakadash && CGO_ENABLED=0 go build ./... && go vet ./...</automated>
    <manual>Create test config without [wakadash] section, run wakadash, verify section is appended with commented template</manual>
  </verify>
  <done>
EnsureWakadashSection function exists and is called on startup. Running wakadash with a config file missing [wakadash] section results in the section being auto-created with commented template including wakasync link.
  </done>
</task>

</tasks>

<verification>
1. Build verification: `cd /workspace/wakadash && CGO_ENABLED=0 go build ./...`
2. Lint verification: `cd /workspace/wakadash && go vet ./...`
3. Config parsing test:
   - Create test config with `[wakadash]\nhistory_repo = test/repo`
   - Verify cfg.HistoryRepo equals "test/repo"
4. Graceful fallback test:
   - Run wakadash without history_repo in config
   - Verify dashboard starts and shows live stats
5. Section auto-creation test:
   - Use config without [wakadash] section
   - Run wakadash
   - Verify [wakadash] section appended with commented template
</verification>

<success_criteria>
1. Config struct includes HistoryRepo string field
2. Load() parses [wakadash] section and extracts history_repo
3. Missing history_repo returns empty string (no error)
4. Dashboard starts normally without history_repo configured
5. EnsureWakadashSection creates template on first run
6. All code compiles without warnings
</success_criteria>

<output>
After completion, create `.planning/phases/11-configuration-validation/11-01-SUMMARY.md`
</output>
