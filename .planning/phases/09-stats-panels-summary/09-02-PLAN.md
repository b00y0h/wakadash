---
phase: 09-stats-panels-summary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wakadash/internal/tui/model.go
  - wakadash/internal/tui/summary_panel.go
  - wakadash/internal/types/types.go
autonomous: true
requirements:
  - STAT-05

must_haves:
  truths:
    - "Dashboard displays Summary panel with Last 30d total"
    - "Dashboard displays Summary panel with daily average"
    - "Dashboard displays Summary panel with top project, editor, category, and OS"
    - "Dashboard displays Summary panel with current and best streak (from 7-day window)"
    - "Dashboard displays Summary panel with language and project counts"
  artifacts:
    - path: "wakadash/internal/tui/summary_panel.go"
      provides: "Summary panel rendering and streak calculation"
      exports: ["renderSummaryPanel", "calculateStreaks"]
    - path: "wakadash/internal/tui/model.go"
      provides: "Model with showSummary visibility flag"
      contains: "showSummary bool"
    - path: "wakadash/internal/types/types.go"
      provides: "Extended StatItem with Percent field"
      contains: "Percent float64"
  key_links:
    - from: "wakadash/internal/tui/summary_panel.go"
      to: "wakadash/internal/tui/model.go"
      via: "Model method accessing stats and summaryData"
      pattern: "func \\(m Model\\) renderSummaryPanel"
    - from: "wakadash/internal/tui/summary_panel.go"
      to: "wakadash/internal/types/types.go"
      via: "Accessing StatItem arrays from StatsData"
      pattern: "m\\.stats\\.Data\\.(Languages|Projects|Editors|Categories)"
---

<objective>
Create Summary panel showing 30-day statistics overview with visual distinction.

Purpose: Provide users with a high-level overview before drilling into individual panels. Shows totals, averages, top items, and streak information.

Output: summary_panel.go with rendering and streak calculation, updated Model with visibility flag and BestDay type support.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-stats-panels-summary/09-RESEARCH.md
@.planning/phases/09-stats-panels-summary/09-CONTEXT.md
@wakadash/internal/tui/model.go
@wakadash/internal/tui/styles.go
@wakadash/internal/types/types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types.go with Percent field and BestDay struct</name>
  <files>wakadash/internal/types/types.go</files>
  <action>
Update types.go:

1. Add Percent field to StatItem struct:
   ```go
   type StatItem struct {
       Name         string  `json:"name"`
       TotalSeconds float64 `json:"total_seconds"`
       Percent      float64 `json:"percent"`  // ADD THIS
   }
   ```
   The WakaTime API returns percent for each item but we weren't capturing it.

2. Add BestDay struct (WakaTime stats response includes best_day):
   ```go
   // BestDay represents the day with the most coding activity.
   type BestDay struct {
       Date         string  `json:"date"`
       Text         string  `json:"text"`
       TotalSeconds float64 `json:"total_seconds"`
   }
   ```

3. Add BestDay field to StatsData struct:
   ```go
   BestDay BestDay `json:"best_day"`
   ```
   Add after the End field, before HumanReadableDailyAverage.

NOTE: The WakaTime API already returns these fields, we just weren't deserializing them.
  </action>
  <verify>
go build ./... compiles without errors.
grep "Percent.*float64" wakadash/internal/types/types.go returns match.
grep "type BestDay struct" wakadash/internal/types/types.go returns match.
  </verify>
  <done>
StatItem has Percent field, BestDay struct defined, StatsData has BestDay field.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create summary_panel.go with rendering and streak calculation</name>
  <files>wakadash/internal/tui/summary_panel.go</files>
  <action>
Create new file summary_panel.go with:

1. Package declaration and imports (fmt, strings, lipgloss, theme)

2. SummaryPanelStyle(t theme.Theme, width int) lipgloss.Style function:
   - Return style with RoundedBorder()
   - BorderForeground(t.Primary) for subtle accent per user decision
   - Padding(1, 2)
   - Width(width - 4) to account for border

3. calculateStreaks(summaryData *types.SummaryResponse) (current int, best int) function:
   - If summaryData nil or empty, return (0, 0)
   - Calculate current streak: count consecutive days with GrandTotal.TotalSeconds > 0 from most recent backwards
   - Calculate best streak: longest consecutive run in available 7-day data
   - Per research: limit to 7-day window (data already available from heatmap)

4. renderSummaryPanel() string method on Model:
   - Use SummaryPanelStyle for wrapper
   - Title: "Summary (Last 30 Days)" using TitleStyle
   - Content lines (each with 2-space indent):
     * Total: m.stats.Data.HumanReadableTotal
     * Daily average: m.stats.Data.HumanReadableDailyAverage
     * Best day: BestDay.Date + " (" + BestDay.Text + ")"
     * Streak: "Current: X days | Best: Y days" from calculateStreaks()
     * Top language: first item from Languages array (if exists)
     * Top project: first item from Projects array (if exists)
     * Top editor: first item from Editors array (if exists)
     * Top category: first item from Categories array (if exists)
     * Languages: count of Languages array
     * Projects: count of Projects array
   - Return styled string

Format following user decision:
- "Current: 5 days | Best: 14 days" for streaks
- Show counts as simple "Languages: 12" format

NOTE: Summary panel positioned at top and spans full width - handled in plan 09-03.
  </action>
  <verify>
go build ./... compiles without errors.
grep "func calculateStreaks" wakadash/internal/tui/summary_panel.go returns match.
grep "func (m Model) renderSummaryPanel" wakadash/internal/tui/summary_panel.go returns match.
grep "SummaryPanelStyle" wakadash/internal/tui/summary_panel.go returns match.
  </verify>
  <done>
summary_panel.go exists with SummaryPanelStyle, calculateStreaks, and renderSummaryPanel functions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add showSummary visibility flag to Model</name>
  <files>wakadash/internal/tui/model.go</files>
  <action>
Update Model in model.go:

1. Add showSummary visibility flag after existing panel flags:
   ```go
   showSummary bool // Summary panel visibility
   ```

2. In NewModel(), initialize showSummary to true (user decision: start with all visible)

NOTE: Do NOT add keyboard handling or View() integration yet - that's in plan 09-03.
  </action>
  <verify>
go build ./... compiles without errors.
grep "showSummary.*bool" wakadash/internal/tui/model.go returns match.
  </verify>
  <done>
Model has showSummary field initialized to true in NewModel().
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd wakadash && go build ./...`
2. StatItem has Percent field for percentage display
3. BestDay struct captures best day data from API
4. calculateStreaks returns current and best from 7-day window
5. renderSummaryPanel produces formatted overview with accent styling
6. showSummary visibility flag exists and initialized
</verification>

<success_criteria>
- go build succeeds with no errors
- types.go has Percent in StatItem and BestDay struct
- summary_panel.go has streak calculation and rendering
- Streak format matches user decision: "Current: X days | Best: Y days"
- Summary panel has accent border per user decision
- showSummary flag initialized to true
</success_criteria>

<output>
After completion, create `.planning/phases/09-stats-panels-summary/09-02-SUMMARY.md`
</output>
