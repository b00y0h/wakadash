---
phase: 09-stats-panels-summary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wakadash/internal/tui/model.go
  - wakadash/internal/tui/stats_panels.go
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04

must_haves:
  truths:
    - "Dashboard displays Categories panel with top 10 items, time labels, and percentages"
    - "Dashboard displays Editors panel with top 10 items, time labels, and percentages"
    - "Dashboard displays Operating Systems panel with top 10 items, time labels, and percentages"
    - "Dashboard displays Machines panel with top 10 items, time labels, and percentages"
  artifacts:
    - path: "wakadash/internal/tui/stats_panels.go"
      provides: "Rendering functions for all four stat panels"
      exports: ["renderCategoriesPanel", "renderEditorsPanel", "renderOSPanel", "renderMachinesPanel", "updateCategoriesChart", "updateEditorsChart", "updateOSChart", "updateMachinesChart"]
    - path: "wakadash/internal/tui/model.go"
      provides: "Model fields for four new barchart models and visibility flags"
      contains: "categoriesChart barchart.Model"
  key_links:
    - from: "wakadash/internal/tui/stats_panels.go"
      to: "wakadash/internal/tui/model.go"
      via: "Model methods for chart rendering"
      pattern: "func \\(m \\*?Model\\) (render|update).*(Categories|Editors|OS|Machines)"
---

<objective>
Add four new stats panels (Categories, Editors, Operating Systems, Machines) using the existing ntcharts barchart pattern.

Purpose: Display comprehensive coding activity breakdown beyond just languages and projects. Users can see which categories (coding, debugging, etc.), editors (VS Code, Neovim, etc.), operating systems, and machines they use most.

Output: stats_panels.go with rendering/update functions, updated Model with chart fields and visibility flags.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-stats-panels-summary/09-RESEARCH.md
@.planning/phases/09-stats-panels-summary/09-CONTEXT.md
@wakadash/internal/tui/model.go
@wakadash/internal/types/types.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stats_panels.go with four panel rendering functions</name>
  <files>wakadash/internal/tui/stats_panels.go</files>
  <action>
Create new file stats_panels.go with:

1. Package declaration and imports (barchart, lipgloss, fmt, strings)

2. formatTimeWithPercent(secs float64, percent float64) string helper:
   - Reuse existing formatSeconds() for time portion
   - Format as "2h 15m (65%)" per user decision
   - Use fmt.Sprintf("%.0f%%", percent) for whole-number percentages

3. Four update*Chart methods on *Model:
   - updateCategoriesChart(): Populate m.categoriesChart from m.stats.Data.Categories
   - updateEditorsChart(): Populate m.editorsChart from m.stats.Data.Editors
   - updateOSChart(): Populate m.osChart from m.stats.Data.OperatingSystems
   - updateMachinesChart(): Populate m.machinesChart from m.stats.Data.Machines

   Each follows the existing updateLanguagesChart() pattern:
   - Clear chart, limit to 10 items (user decision)
   - Use theme.Primary color for bars (consistent styling)
   - Label format: "ItemName: 2h 15m (65%)" using formatTimeWithPercent
   - Push BarData with hours as Value

4. Four render*Panel methods on Model:
   - renderCategoriesPanel() string
   - renderEditorsPanel() string
   - renderOSPanel() string
   - renderMachinesPanel() string

   Each follows existing renderStats() panel pattern:
   - TitleStyle(m.theme).Render("Panel Title") + newline
   - Check if data exists, show "No data" if empty
   - Return chart.View() output

NOTE: Use theme.Primary color for all bars to match consistent styling (per user context: "All panels use consistent visual styling with selected theme").
  </action>
  <verify>
go build ./... compiles without errors.
grep -c "func (m \*Model) update.*Chart" wakadash/internal/tui/stats_panels.go returns 4.
grep -c "func (m Model) render.*Panel" wakadash/internal/tui/stats_panels.go returns 4.
  </verify>
  <done>
stats_panels.go exists with formatTimeWithPercent helper, 4 update methods, and 4 render methods.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add chart fields and visibility flags to Model</name>
  <files>wakadash/internal/tui/model.go</files>
  <action>
Update Model struct in model.go:

1. Add four new barchart.Model fields after existing projectsChart:
   ```go
   categoriesChart barchart.Model
   editorsChart    barchart.Model
   osChart         barchart.Model
   machinesChart   barchart.Model
   ```

2. Add four visibility flags after existing showHeatmap:
   ```go
   showCategories bool // 5 key (will be mapped in plan 09-03)
   showEditors    bool // 6 key
   showOS         bool // 7 key
   showMachines   bool // 8 key
   ```

3. In NewModel(), initialize the four new charts:
   - Use barchart.New(35, 10) for each (35 width, 10 height for top 10 items)
   - Set all four visibility flags to true (user decision: start with all visible)

4. In Update() case tea.WindowSizeMsg:
   - Add Resize() calls for all four new charts (panelWidth, 10)

5. In Update() case statsFetchedMsg:
   - Add calls to m.updateCategoriesChart(), m.updateEditorsChart(), m.updateOSChart(), m.updateMachinesChart()

NOTE: Do NOT add keyboard handling yet - that's in plan 09-03.
NOTE: Do NOT integrate into View() yet - that's in plan 09-03 (layout refactoring).
  </action>
  <verify>
go build ./... compiles without errors.
grep "categoriesChart barchart.Model" wakadash/internal/tui/model.go returns match.
grep "showCategories.*bool" wakadash/internal/tui/model.go returns match.
  </verify>
  <done>
Model has 4 new chart fields, 4 visibility flags, charts initialized in NewModel(), resized on WindowSizeMsg, and updated on statsFetchedMsg.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd wakadash && go build ./...`
2. All four chart fields exist in Model struct
3. All four visibility flags exist and initialized to true
4. All four update methods populate charts from stats data
5. All four render methods produce formatted output with titles
6. formatTimeWithPercent produces "2h 15m (65%)" format
</verification>

<success_criteria>
- go build succeeds with no errors
- stats_panels.go contains 4 update and 4 render functions
- Model struct has 4 new barchart fields and 4 visibility flags
- Charts are initialized, resized, and updated through the existing lifecycle
- Time formatting matches user decision: "2h 15m (65%)"
</success_criteria>

<output>
After completion, create `.planning/phases/09-stats-panels-summary/09-01-SUMMARY.md`
</output>
