---
phase: 06-data-visualization-and-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wakadash/internal/tui/colors.go
  - wakadash/internal/tui/model.go
  - wakadash/internal/tui/styles.go
  - wakadash/go.mod
  - wakadash/go.sum
autonomous: true

must_haves:
  truths:
    - "Dashboard displays languages bar chart with distinct colors per language"
    - "Dashboard displays projects bar chart showing time breakdown"
    - "Chart colors match GitHub Linguist palette for recognized languages"
  artifacts:
    - path: "wakadash/internal/tui/colors.go"
      provides: "Language-to-color mapping using GitHub Linguist palette"
      exports: ["getLanguageColor"]
    - path: "wakadash/internal/tui/model.go"
      provides: "Bar chart rendering for languages and projects"
      contains: "barchart.Model"
  key_links:
    - from: "wakadash/internal/tui/model.go"
      to: "wakadash/internal/tui/colors.go"
      via: "getLanguageColor call for language bar styling"
      pattern: "getLanguageColor\\("
    - from: "wakadash/internal/tui/model.go"
      to: "github.com/NimbleMarkets/ntcharts/barchart"
      via: "bar chart rendering"
      pattern: "barchart\\.New|barchart\\.Model"
---

<objective>
Implement horizontal bar charts for languages and projects using ntcharts, with GitHub Linguist colors for language differentiation.

Purpose: Provide visual representation of coding activity breakdown by language and project, replacing plain-text lists with color-coded charts that are easier to scan.

Output: Dashboard renders languages and projects as horizontal bar charts in a 2-column layout.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-visualization-and-ux/06-RESEARCH.md
@.planning/phases/05-tui-foundation/05-02-SUMMARY.md

Key patterns from research:
- ntcharts barchart.Model for horizontal bars with lipgloss styling
- barchart.Push(BarData{}) to add data, then barchart.Draw() to render
- barchart.Resize(w, h) for responsive sizing
- GitHub Linguist colors for language-specific styling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ntcharts dependency and create colors.go</name>
  <files>wakadash/go.mod, wakadash/go.sum, wakadash/internal/tui/colors.go</files>
  <action>
1. Add ntcharts dependency:
   ```bash
   cd /workspace/wakadash
   go get github.com/NimbleMarkets/ntcharts@latest
   ```

2. Create `internal/tui/colors.go` with GitHub Linguist color mapping:
   - Package tui
   - Import lipgloss for Color type
   - Create `languageColors` map with at least 15 common languages:
     Go (#00ADD8), JavaScript (#f1e05a), TypeScript (#3178c6), Python (#3572A5),
     Rust (#dea584), Ruby (#701516), Java (#b07219), C (#555555), C++ (#f34b7d),
     HTML (#e34c26), CSS (#563d7c), Shell (#89e051), Markdown (#083fa1),
     PHP (#4F5D95), Swift (#F05138)
   - Create `getLanguageColor(name string) lipgloss.Color` function
   - Use case-insensitive matching (compare lowercase)
   - Return default gray (#cccccc) for unknown languages

3. Run `go mod tidy` to clean up dependencies.
  </action>
  <verify>
- `go build ./...` compiles without errors
- `grep -q "getLanguageColor" wakadash/internal/tui/colors.go` returns 0
- `grep -q "ntcharts" wakadash/go.mod` returns 0
  </verify>
  <done>colors.go exists with getLanguageColor function and 15+ language color mappings</done>
</task>

<task type="auto">
  <name>Task 2: Add bar chart models to Model and implement rendering</name>
  <files>wakadash/internal/tui/model.go, wakadash/internal/tui/styles.go</files>
  <action>
1. In model.go, add to Model struct:
   ```go
   // Chart models (ntcharts)
   languagesChart barchart.Model
   projectsChart  barchart.Model
   ```

2. In NewModel(), initialize chart models:
   ```go
   languagesChart := barchart.New(35, 8)  // Width, height for initial size
   projectsChart := barchart.New(35, 8)
   ```
   Note: Actual sizing will be handled in View() based on terminal dimensions.

3. Create helper methods for chart updates:
   - `updateLanguagesChart()` - Clears chart, iterates over stats.Data.Languages (top 5),
     calls getLanguageColor(lang.Name), pushes BarData with value = lang.TotalSeconds/3600.0 (hours),
     calls Draw()
   - `updateProjectsChart()` - Similar for stats.Data.Projects, uses a fixed color (e.g., cyan #00d7ff)

4. In Update(), after statsFetchedMsg processing:
   - Call m.updateLanguagesChart()
   - Call m.updateProjectsChart()

5. Replace renderStats() implementation:
   - Create 2-column layout using lipgloss.JoinHorizontal
   - Left panel: "Languages" title + m.languagesChart.View()
   - Right panel: "Projects" title + m.projectsChart.View()
   - Calculate panel widths based on m.width (each panel gets ~half width minus borders/padding)
   - Keep existing totals section (total time, daily average) above the charts

6. In WindowSizeMsg handler, resize charts:
   ```go
   panelWidth := (msg.Width / 2) - 4
   chartHeight := 8  // Fixed height for bar charts
   m.languagesChart.Resize(panelWidth, chartHeight)
   m.projectsChart.Resize(panelWidth, chartHeight)
   ```

7. In styles.go, add chart panel style if needed (or reuse borderStyle with adjusted width).

Import required:
```go
"github.com/NimbleMarkets/ntcharts/barchart"
```
  </action>
  <verify>
- `CGO_ENABLED=0 go build -o wakadash ./cmd/wakadash` compiles successfully
- Run `./wakadash` (requires valid API key in ~/.wakatime.cfg) - should display two bar charts side by side
- Languages chart shows colored bars, projects chart shows uniform-color bars
  </verify>
  <done>Dashboard displays languages and projects as horizontal bar charts with GitHub Linguist colors for languages</done>
</task>

</tasks>

<verification>
1. Build check: `CGO_ENABLED=0 go build -o /workspace/wakadash/wakadash /workspace/wakadash/cmd/wakadash`
2. Lint check: `cd /workspace/wakadash && gofmt -d internal/tui/` (should have no output)
3. Integration test: Manual run of `./wakadash` showing bar charts
4. Color verification: Languages bar chart uses distinct colors (visually different bars)
</verification>

<success_criteria>
- Dashboard renders languages bar chart with color-coded bars matching GitHub Linguist palette
- Dashboard renders projects bar chart with time breakdown
- Charts are side-by-side in 2-column layout
- Charts resize correctly when terminal size changes (no cut-off or overflow)
- All existing functionality preserved (stats display, refresh, help overlay)
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-visualization-and-ux/06-01-SUMMARY.md`
</output>
