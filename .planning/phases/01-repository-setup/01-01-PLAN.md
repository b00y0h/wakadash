---
phase: 01-repository-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Fork b00y0h/wakafetch exists on GitHub"
    - "Fork has wiki and projects disabled"
    - "Fork description indicates purpose"
  artifacts:
    - path: "github.com/b00y0h/wakafetch"
      provides: "Fork repository"
      type: "github-repo"
  key_links:
    - from: "b00y0h/wakafetch"
      to: "sahaj-b/wakafetch"
      via: "GitHub fork relationship"
      pattern: "forked from sahaj-b/wakafetch"
---

<objective>
Fork the upstream wakafetch repository to b00y0h/wakafetch and configure it for independent development.

Purpose: Create the fork that will contain GoReleaser config and GitHub Actions workflow for automated releases.
Output: Configured fork repository with wiki and projects disabled.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-setup/01-CONTEXT.md
@.planning/phases/01-repository-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork upstream repository</name>
  <files>N/A (GitHub operation)</files>
  <action>
    Pre-flight: Verify GitHub CLI authentication with `gh auth status`.

    Fork sahaj-b/wakafetch to personal account using:
    ```bash
    gh repo fork sahaj-b/wakafetch --fork-name wakafetch --clone=false
    ```

    Use `--clone=false` to create fork on GitHub only (no local clone needed).

    Verify fork created with:
    ```bash
    gh repo view b00y0h/wakafetch --json name,owner,isFork,parent
    ```

    Per Claude's discretion (from CONTEXT.md): Update fork description to clarify purpose:
    ```bash
    gh repo edit b00y0h/wakafetch --description "Personal fork of wakafetch for automated Homebrew releases"
    ```
  </action>
  <verify>
    `gh repo view b00y0h/wakafetch --json isFork,parent` returns `{"isFork":true,"parent":{"owner":"sahaj-b","name":"wakafetch"}}`
  </verify>
  <done>Fork b00y0h/wakafetch exists as a fork of sahaj-b/wakafetch with custom description</done>
</task>

<task type="auto">
  <name>Task 2: Configure fork settings</name>
  <files>N/A (GitHub operation)</files>
  <action>
    Disable unused features on fork per locked decision:
    ```bash
    gh repo edit b00y0h/wakafetch \
      --enable-wiki=false \
      --enable-projects=false
    ```

    Note: `--enable-projects=false` only disables Projects (classic). Modern Projects remain enabled via CLI limitation. This is acceptable per research recommendations.

    Verify issues and actions remain enabled (default behavior, no action needed).

    Verify settings applied:
    ```bash
    gh repo view b00y0h/wakafetch --json hasWikiEnabled,hasProjectsEnabled
    ```
  </action>
  <verify>
    `gh repo view b00y0h/wakafetch --json hasWikiEnabled,hasProjectsEnabled` returns `{"hasWikiEnabled":false,"hasProjectsEnabled":false}`
  </verify>
  <done>Fork has wiki disabled and projects (classic) disabled</done>
</task>

</tasks>

<verification>
1. `gh repo view b00y0h/wakafetch --json isFork` confirms fork exists
2. `gh repo view b00y0h/wakafetch --json hasWikiEnabled,hasProjectsEnabled` shows both false
3. Fork description shows "Personal fork of wakafetch for automated Homebrew releases"
</verification>

<success_criteria>
- Fork b00y0h/wakafetch exists and is linked to upstream sahaj-b/wakafetch
- Wiki and Projects (classic) are disabled
- Issues and Actions remain enabled
- Fork description clarifies purpose
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-setup/01-01-SUMMARY.md`
</output>
