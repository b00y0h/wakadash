---
phase: 01-repository-setup
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Fine-grained PAT exists with Contents read/write on homebrew-wakafetch"
    - "HOMEBREW_TAP_TOKEN secret is configured in b00y0h/wakafetch"
    - "Secret can be used by GitHub Actions workflows"
  artifacts:
    - path: "github.com/b00y0h/wakafetch/settings/secrets/actions/HOMEBREW_TAP_TOKEN"
      provides: "Repository secret for tap publishing"
      type: "github-secret"
  key_links:
    - from: "HOMEBREW_TAP_TOKEN secret"
      to: "Fine-grained PAT"
      via: "GitHub Actions secret storage"
      pattern: "gh secret set HOMEBREW_TAP_TOKEN"
    - from: "PAT permissions"
      to: "homebrew-wakafetch repo"
      via: "Repository-scoped Contents read/write"
      pattern: "Only select repositories.*homebrew-wakafetch"
---

<objective>
Create fine-grained Personal Access Token and configure it as a repository secret for cross-repo publishing.

Purpose: Enable GitHub Actions in the fork to push formula updates to the tap repository.
Output: HOMEBREW_TAP_TOKEN secret configured in b00y0h/wakafetch repository.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-setup/01-CONTEXT.md
@.planning/phases/01-repository-setup/01-RESEARCH.md
@.planning/phases/01-repository-setup/01-01-SUMMARY.md
@.planning/phases/01-repository-setup/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create fine-grained Personal Access Token</name>
  <files>N/A (GitHub web UI operation)</files>
  <action>
    Human action required: Create fine-grained PAT via GitHub web interface.
    This cannot be automated - GitHub requires web UI authentication for token creation.

    Navigate to: https://github.com/settings/personal-access-tokens/new

    Configure token with these EXACT settings:
    - Token name: HOMEBREW_TAP_TOKEN
    - Expiration: 1 year (per locked decision)
    - Resource owner: b00y0h (your account)
    - Repository access: Select "Only select repositories"
    - Select repositories: Choose ONLY homebrew-wakafetch

    Set permissions:
    - Repository permissions:
      - Contents: Read and write (required for pushing formula updates)
      - Metadata: Read-only (automatically included)
    - Leave all other permissions unselected

    Click "Generate token" and copy immediately (shown only once).
    Store temporarily in a secure location (password manager recommended).

    Security notes:
    - Do NOT select "All repositories" - token should ONLY access homebrew-wakafetch
    - Do NOT add additional permissions beyond Contents and Metadata
    - 1-year expiration means rotation needed in Feb 2027
  </action>
  <verify>
    User confirms "token ready" indicating token was created and copied.
  </verify>
  <done>Fine-grained PAT created with Contents read/write scope on homebrew-wakafetch only</done>
</task>

<task type="auto">
  <name>Task 2: Configure repository secret</name>
  <files>N/A (GitHub operation)</files>
  <action>
    After user confirms token is ready, prompt for secure input:

    The token must be passed securely. Use interactive mode:
    ```bash
    gh secret set HOMEBREW_TAP_TOKEN --repo b00y0h/wakafetch
    ```

    This will prompt for the secret value without echoing to terminal.

    Alternative if interactive input not available, instruct user to run:
    ```bash
    echo "YOUR_TOKEN_HERE" | gh secret set HOMEBREW_TAP_TOKEN --repo b00y0h/wakafetch
    ```
    (User replaces YOUR_TOKEN_HERE with actual token)

    Verify secret was created:
    ```bash
    gh secret list --repo b00y0h/wakafetch
    ```

    Expected output should include `HOMEBREW_TAP_TOKEN` in the list.
  </action>
  <verify>
    `gh secret list --repo b00y0h/wakafetch` output includes `HOMEBREW_TAP_TOKEN`
  </verify>
  <done>HOMEBREW_TAP_TOKEN secret is configured and ready for use by GitHub Actions</done>
</task>

</tasks>

<verification>
1. `gh secret list --repo b00y0h/wakafetch` shows HOMEBREW_TAP_TOKEN in list
2. Token was created with correct scope (user verified during creation)
3. Token expiration is set to 1 year
</verification>

<success_criteria>
- Fine-grained PAT created with Contents read/write on homebrew-wakafetch only
- Token has 1-year expiration
- HOMEBREW_TAP_TOKEN secret exists in b00y0h/wakafetch repository
- Secret is available for Actions workflows (verified via secret list)
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-setup/01-03-SUMMARY.md`
</output>
