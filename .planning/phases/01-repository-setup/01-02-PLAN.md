---
phase: 01-repository-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Tap repository b00y0h/homebrew-wakafetch exists on GitHub"
    - "Tap has Casks/ directory structure"
    - "Tap has MIT LICENSE file"
    - "Tap has README with installation instructions"
  artifacts:
    - path: "github.com/b00y0h/homebrew-wakafetch"
      provides: "Homebrew tap repository"
      type: "github-repo"
    - path: "homebrew-wakafetch/Casks/"
      provides: "Cask directory for formula"
      type: "directory"
    - path: "homebrew-wakafetch/LICENSE"
      provides: "MIT license"
      type: "file"
    - path: "homebrew-wakafetch/README.md"
      provides: "Installation instructions"
      type: "file"
  key_links:
    - from: "brew tap b00y0h/wakafetch"
      to: "github.com/b00y0h/homebrew-wakafetch"
      via: "Homebrew tap naming convention"
      pattern: "homebrew-wakafetch"
---

<objective>
Create the Homebrew tap repository with proper structure for cask distribution.

Purpose: Establish the tap repository that GoReleaser will push formulas to.
Output: Public tap repository with Casks/ directory, LICENSE, and README.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-setup/01-CONTEXT.md
@.planning/phases/01-repository-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tap repository</name>
  <files>N/A (GitHub operation)</files>
  <action>
    Pre-flight: Verify GitHub CLI authentication with `gh auth status`.

    Create public tap repository with selective features:
    ```bash
    gh repo create homebrew-wakafetch \
      --public \
      --description "Homebrew tap for wakafetch - a modern system information tool" \
      --disable-wiki=true \
      --license MIT
    ```

    Disable projects feature (post-creation):
    ```bash
    gh repo edit b00y0h/homebrew-wakafetch --enable-projects=false
    ```

    Verify repository created:
    ```bash
    gh repo view b00y0h/homebrew-wakafetch --json name,visibility,hasWikiEnabled
    ```
  </action>
  <verify>
    `gh repo view b00y0h/homebrew-wakafetch --json name,visibility` returns `{"name":"homebrew-wakafetch","visibility":"PUBLIC"}`
  </verify>
  <done>Tap repository b00y0h/homebrew-wakafetch exists as public repo</done>
</task>

<task type="auto">
  <name>Task 2: Initialize tap directory structure</name>
  <files>Casks/.gitkeep, LICENSE, README.md</files>
  <action>
    Clone the newly created tap repository:
    ```bash
    gh repo clone b00y0h/homebrew-wakafetch /tmp/homebrew-wakafetch
    cd /tmp/homebrew-wakafetch
    ```

    Create Casks directory with placeholder:
    ```bash
    mkdir -p Casks
    touch Casks/.gitkeep
    ```

    The LICENSE file was created by `gh repo create --license MIT`.
    Verify it exists, if not create MIT license.

    Create minimal README.md per Claude's discretion (start minimal):
    ```bash
    cat > README.md << 'EOF'
    # Homebrew Tap for wakafetch

    Homebrew tap for [wakafetch](https://github.com/b00y0h/wakafetch) - a modern system information tool.

    ## Installation

    ```bash
    brew tap b00y0h/wakafetch
    brew install wakafetch
    ```

    ## About

    This tap provides the wakafetch formula for Homebrew installation on macOS and Linux.

    ## License

    MIT
    EOF
    ```

    Commit and push:
    ```bash
    git add .
    git commit -m "Initialize tap structure with Casks directory and README"
    git push origin main
    ```

    Clean up local clone:
    ```bash
    rm -rf /tmp/homebrew-wakafetch
    ```
  </action>
  <verify>
    `gh api repos/b00y0h/homebrew-wakafetch/contents/Casks` returns directory listing (200 OK)
    `gh api repos/b00y0h/homebrew-wakafetch/contents/README.md` returns file metadata (200 OK)
    `gh api repos/b00y0h/homebrew-wakafetch/contents/LICENSE` returns file metadata (200 OK)
  </verify>
  <done>Tap has Casks/ directory, LICENSE, and README.md committed</done>
</task>

</tasks>

<verification>
1. `gh repo view b00y0h/homebrew-wakafetch` shows repository exists
2. `gh api repos/b00y0h/homebrew-wakafetch/contents` lists Casks/, LICENSE, README.md
3. Repository is public and has MIT license
</verification>

<success_criteria>
- Tap repository b00y0h/homebrew-wakafetch exists and is public
- Casks/ directory exists (ready for formula in later phase)
- LICENSE file contains MIT license
- README.md contains installation instructions
- Wiki and Projects (classic) disabled
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-setup/01-02-SUMMARY.md`
</output>
